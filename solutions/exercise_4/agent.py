"""
Exercise 4: Agent Evaluation

Switch the import below between flawed_agent and fixed_agent to see
how evaluation scores change based on prompt engineering.
"""

# Step 1: Start with the flawed agent, run the eval, and observe failures
from .flawed_agent import root_agent

# Step 2: Switch to the fixed agent and re-run the eval to see it pass
# from .fixed_agent import root_agent
